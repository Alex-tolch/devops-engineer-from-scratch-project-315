---
- name: Configure Docker and firewall
  hosts: VM
  become: true

  pre_tasks:
    - name: Assert Ubuntu or Debian
      ansible.builtin.assert:
        that:
          - ansible_distribution in ['Ubuntu', 'Debian']
        fail_msg: This playbook supports Ubuntu and Debian only.

  roles:
    - docker_host
    - ufw
