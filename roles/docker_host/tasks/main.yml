---
- name: Install Python Docker SDK for Ansible community.docker modules
  ansible.builtin.apt:
    name: "{{ docker_host_python_docker_package }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
